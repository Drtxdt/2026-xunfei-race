# 国赛
```bash
source devel/setup.bash
# 左线
roslaunch ucar_2026_track_end_stop track_end_stop.launch
# 右线
roslaunch ucar_2026_track_end_stop right_track_end_stop.launch
# 中线
roslaunch ucar_2026_track_end_stop stable_right_track_end_stop.launch
完成状态分别是 `/track_end_stop/status=finish`、`/right_track_end_stop/status=right_finish`、`/stable_right_track_end_stop/status=stable_right_finish`。

正式流程里不用手动选——task4 红绿灯出结果后 task5 自动拉起 launch(`competition_flow.py:2823-2837`)；也可以绕过识别直接给参数 `traffic_decision:=left/right/straight` 让 task5 跑指定那条。另外注意省赛包 `_provincial` 里也有同名三个，区别就是没有避障。
```


# 省赛
```bash
source devel/setup.bash

# 左线
roslaunch ucar_2026_track_end_stop_provincial track_end_stop.launch

# 右线
roslaunch ucar_2026_track_end_stop_provincial right_track_end_stop.launch

# 中线（straight）
roslaunch ucar_2026_track_end_stop_provincial stable_right_track_end_stop.launch
```