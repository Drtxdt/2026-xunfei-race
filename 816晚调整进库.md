0.55的parking_staging_offset改为0.45吧，parking_dock_min_yaw: 改为0.25，parking_goal_offset改为0.22，camera_boresight_yaw_offset这个我再试试

这些参数我都不是很想调整了，这样吧，还记得我之前设置的每个点的时间阈值吗？coverage_goal_soft_timeout_sec: 7.0
coverage_goal_hard_timeout_sec: 10.0。
我想这样：计算当前车位置和目标点的位置距离，如果距离小于一个阈值，那么开始计时，从此刻开始，coverage_goal_soft_timeout_sec时间内没有成功抵达，说明航点被障碍物占用，直接跳过
然后再计算每个导航点，发航点到当前时间的时间差，如果大于coverage_goal_hard_timeout_sec，那么跳过当前点